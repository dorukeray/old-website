{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "publish": "yarn node publish",
    "warmup": "yarn install && nuxt prepare && nuxt dev"
  },
  "devDependencies": {
    "ftp-deploy": "^2.4.3",
    "nuxt": "3.0.0-rc.9",
    "nuxt-icon": "^0.1.5",
    "sass": "^1.54.0",
    "sass-loader": "^13.0.2",
    "twemoji": "^14.0.2"
  },
  "packageManager": "yarn@3.2.3"
}
