{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "publish": "yarn node publish",
    "warmup": "yarn install && nuxt prepare"
  },
  "devDependencies": {
    "dotenv": "^16.0.2",
    "ftp-deploy": "^2.4.3",
    "nuxt": "rc",
    "nuxt-icon": "^0.1.5",
    "sass": "^1.54.0",
    "sass-loader": "^13.0.2",
    "twemoji": "^14.0.2"
  },
  "packageManager": "yarn@3.2.3"
}
